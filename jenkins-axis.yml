build_name:
  - digital-identity
  - usercontract_bdd_3-3
test_dir:
  - Digital-Identity
  - UserContract

exclude:
  - build_name: digital-identity
    test_dir: UserContract
  - build_name: usercontract_bdd_3-3
    test_dir: UserContract

execution-strategy:
  - sequential: true

builders:
  - shell: cd $BASE_DIR/bdd && chmod +x jenkins.sh && ./jenkins.sh